scarcity-simulation project

## 1. Quiz (10 marks, 40 questions)
This is likely a general AI/ML/DL/NLP concepts quiz, not project-specific — probably testing what you've learned in coursework. Nothing to build here, just revise: supervised vs. unsupervised learning, RL basics (agent/action/reward/policy), transformer/LLM basics, NLP fundamentals (tokenization, embeddings), evaluation metrics (precision/recall/F1), and basic statistics (t-test, p-value) since those show up in your project too. If you want, I can generate a practice quiz covering these topics.

## 2. Project (10 marks) — broken into 4 sub-parts

**Problem Identification (2 marks)**
This is your problem statement — you already have it refined from our earlier conversation. Use this, tightened to a paragraph:
> "There is no rigorous, quantitative comparison of how LLM-driven AI agents behave versus humans under identical resource-scarcity conditions. This project builds a controlled multi-agent simulation to measure behavioral divergence (cooperation, hoarding, deception) between AI agents and human participants in matched scarcity scenarios."

**Dataset (2 marks)**
Since your data is *generated*, not pre-existing, describe it as such:
- AI trial logs: generated by running your simulation with LLM/RL agents across scenarios (drought, repeated-trust, etc.)
- Human trial logs: collected via your Streamlit participant interface
- Both logged in the same schema (action, resource state, messages, timestamps)
- Mention target size: 25-30 human participants × multiple rounds, plus 50-100 AI trials per scenario

At this initial stage, you likely won't have full data yet — that's fine, describe your **data collection plan** and show a **sample schema/mock data** (I can help you generate a few sample rows).

**Data Preprocessing (2 marks)**
Explain what you'll do to raw logs before analysis:
- Parsing JSON action logs into structured tabular format
- Handling missing/incomplete trials (e.g., a participant who quit early)
- Normalizing resource values across different scenario configs
- Extracting derived features (hoarding index, sharing rate, Gini coefficient) from raw action sequences

**EDA (2 marks)**
Even with a small pilot batch, show some exploratory analysis:
- Distribution of actions taken (bar chart: gather vs. share vs. hoard counts)
- Resource levels over rounds (line chart)
- Survival rate across scenarios
- If you have even a handful of pilot trials (AI-only, run a few yourself first), use real numbers here rather than hypothetical ones — much stronger for marks

**Model Identification (2 marks)**
Name your actual model components clearly:
- RL policy: PPO (via Stable-Baselines3) for reflexive agent actions
- LLM reasoning module: [your chosen LLM] for high-level social decisions (share/hoard/ally/deceive) and natural language communication
- Analysis model: classifier (logistic regression / random forest) for the AI-vs-human distinguishability task, with SHAP for interpretability

## 3. Research Paper (10 marks) — Introduction + Literature Survey

**Introduction (5 marks)**
You already have a strong draft from earlier — I wrote this out in the roadmap doc. It covers: the gap, why it matters, your contribution.

**Literature Survey (5 marks)**
You need to write this now — I haven't built this out yet. It should cover 3 areas with actual citations:
1. Generative multi-agent LLM systems (e.g., Park et al.'s "Generative Agents" / Smallville)
2. Multi-agent reinforcement learning under scarcity/survival (MADDPG-style work, PettingZoo environments)
3. Behavioral economics human experiments on scarcity (public goods games, commons-dilemma studies, Ostrom's work)

For each, state what they did and explicitly what's missing that your project addresses.

---

